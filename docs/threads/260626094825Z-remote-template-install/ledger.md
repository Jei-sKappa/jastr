# Lifecycle: remote template install

tier: 2 @ 260626094825Z — new install command that fetches remote templates into `.jastr/`; carries design decisions (source resolution, local/global target, naming/placement, update & conflict handling, trust of remote content) but is a contained feature, not a multi-week architectural shift
