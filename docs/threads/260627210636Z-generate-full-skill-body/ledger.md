# Lifecycle: fully-rendered self-contained skill generation target

tier: 2 @ 260627210636Z — feature carrying a design decision (new self-contained skill generation target: `targets.skill` vs. a `generate agent-skill --inline` flag vs. another shape, whether `argument-hint` is omitted for a render-time target, and the unresolved-required-input hard-error semantics)

closed: done @ 260628142532Z — inline `--mode` spec implemented and verified (full gate green: check/typecheck/test/e2e/living-docs/build); all reviews disposed accepted; External: none, no ticket to close
