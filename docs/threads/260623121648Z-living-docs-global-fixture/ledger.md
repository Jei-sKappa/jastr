# Lifecycle: living-docs generator ignores `global-fixture/`

tier: 1 @ 260623121648Z — contained fix to the docs generator (read global-fixture/, render a second root); the e2e behavior already works, only BEHAVIOR.md rendering is wrong; minor presentation choice, low blast radius
